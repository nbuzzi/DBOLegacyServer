# RespawnExceptions.cfg
# Place world IDs, mob table IDs, and NPC table IDs that should always respawn
# even when the world is dynamic (dungeon). Lines starting with # or ; are comments.
# Multiple values can be comma-separated or space-separated.

# Worlds that always allow respawn
aLLowWorldIds=2001, 2002 3000

# Mob tblidx that always allow respawn
MobIds=68131399, 68131398, 68131397

# NPC tblidx that always allow respawn
NpcIds=5001, 5002
